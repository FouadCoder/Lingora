name: lingora
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  easy_localization: ^3.0.8
  go_router: ^15.1.2
  salomon_bottom_bar: ^3.3.2
  # intl: ^0.19.0
  # percent_indicator: ^4.2.5
  # supabase_flutter: ^2.10.0  # Temporarily commented out due to app_links compatibility issue
  # connectivity_plus: ^6.1.5
  # flutter_staggered_grid_view: ^0.7.0
  # url_launcher: ^6.3.1
  # uuid: ^4.5.1
  # shimmer: ^3.0.0
  # lottie: ^3.2.0
  # hive: ^2.2.3
  # hive_flutter: ^1.1.0
  # flutter_launcher_icons: ^0.14.4
  # flutter_native_splash: ^2.4.4
  # speech_to_text: ^7.0.0
  # audioplayers: ^6.4.0
  # awesome_notifications: ^0.10.0
  # google_generative_ai: ^0.4.7
  # onesignal_flutter: ^5.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/translations/
